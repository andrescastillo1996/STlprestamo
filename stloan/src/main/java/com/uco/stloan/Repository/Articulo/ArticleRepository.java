package com.uco.stloan.Repository.Articulo;

import com.uco.stloan.model.articulo.Article;

public class ArticleRepository extends Article {
    public ArticleRepository(String ref, String nombre, int cantidad ) {
        super(ref, nombre, cantidad);
    }
}
